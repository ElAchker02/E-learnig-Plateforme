{% extends 'base.html.twig' %}

{% block title %}Hello ChapitreController!{% endblock %}

{% block body %}
       <div class="pagetitle d-flex justify-content-between align-items-center">
            <h1>List des chapitres</h1>
            {% if 'ENSEIGNANT' in app.session.get('roles') %}
                      <a href="{{path('add_chapitre')}}" class="btn btn-primary"><i class="fa-solid fa-plus"></i></a>
            {% endif %}
            
        </div>
        {# <div class ="card-body">
        <table class="table table-hover">
                <thead>
                  <tr>
                    <th scope="col">Nom du chapitre</th>
                    <th scope="col">Description</th>
                    <th scope="col">Video</th>
                    <th scope="col">Document</th>
                    <th scope="col">Actions</th>
                  </tr>
                </thead>
                <tbody>
                {% for chap in chapitres %}
                  <tr>
                    <th scope="row">{{ chap.nomChap}}</th>
                    <td>{{chap.description}}</td>
                    <td>{{ chap.video}}</td>
                    <td>{{chap.documents}}</td>
                    <td> <a href="" class="btn btn-primary "><i class="fa fa-file-edit"></i></a>
                      <a href="" class="btn btn-danger"><i class="fa fa-trash"></i></a></td>
                  </tr>
                {% endfor %}
                </tbody>
        </table>
        </div> #}
          <div class="row">
      {% for chap in chapitres %}
          
            <div class="col-sm">
              <div class="card">
                <div class="card-body">
                  <div class="d-flex justify-content-between">
                    <h5 class="card-title">{{ chap.nomChap}}</h5>
                    {% if 'ENSEIGNANT' in app.session.get('roles') %}
                        <div class="actions d-flex justify-content-between align-items-center gap-2">
                          <a href="{{ path('edit_chapitre',{id : chap.id}) }}" class="btn btn-primary "><i class="fa fa-file-edit"></i></a>
                          <a href="{{ path('delete_chapitre',{id : chap.id}) }}" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                        </div>
                    {% endif %}
                  </div>
                  <hr>
                  <p>{{chap.description}}</p>
                  <hr>
                  <p> {% if chap.video != null %}
                            {{chap.video}}
                      {% else %}
                            Le chapitre na pas de video
                      {% endif %}
                    </p>    
                  <hr>
                  <p>{% if chap.documents != null %}
                            {{chap.documents}}
                      {% else %}
                            Le chapitre na pas de document
                      {% endif %}
                  </p>
                  <hr>
                   <p>Appartient aux cours <b> {{chap.nomCours}}</b></p>
                </div>
               
              </div>
            </div>
         
      {% endfor %}
    </div>


{% endblock %}
