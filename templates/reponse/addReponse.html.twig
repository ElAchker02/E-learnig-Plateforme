{% extends 'base.html.twig' %}



{% block body %}


<div class="example-wrapper">

    <div class="pagetitle">
        <h1>Ajouter une reponse à une question</h1>
    </div>
    {# {{ form_start(reponseForm) }}
                        {{ form_row(reponseForm.description , {label : "Description "}) }}
                        {{ form_row(reponseForm.valide , {label : "Valide "}) }}
                        {{ form_row(reponseForm.id_Question , {label : "Question "}) }}
    {{ form_end(reponseForm) }} #}
    {{ form(reponseForm) }}
</div>

{% endblock %}
